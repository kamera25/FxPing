import React from 'react';

const OKSettings: React.FC = () => {
    return (
        <div style={{ padding: '40px', textAlign: 'center', opacity: 0.5 }}>
            この設定項目は準備中です
        </div>
    );
};

export default OKSettings;
